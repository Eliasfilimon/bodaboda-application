# Mosquitto MQTT Broker Configuration
# Bodaboda Application

# Allow connections on default MQTT port
listener 1883

# Allow anonymous connections (no auth needed for demo)
allow_anonymous true

# Logging
log_type all
log_dest stdout

# Persistence
persistence true
persistence_location /mosquitto/data/
